export enum TransactionError {
  INVALID_ADDRESS = "Address is invalid.",
  MISSING_PROPS = "Request is missing parameters.",
  INSUFFICIENT_FUNDS = "Sender account has insufficient funds.",
  NO_CONNECTED_WALLET = "No wallet connected.",
}

export enum UserAccountError {
  INVALID_INPUTS = "Please review your entries.",
}
